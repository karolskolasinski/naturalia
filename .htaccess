RewriteEngine On

# wymuszenie HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# usunięcie www
RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
RewriteRule ^ https://%1%{REQUEST_URI} [L,R=301]

# wyłączanie zbędnych ustawień
SetEnv REGISTER_GLOBALS 0
